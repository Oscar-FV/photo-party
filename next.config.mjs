/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'source.unsplash.com', 'picsum.photos','127.0.0.1', 'localhost', '2cb6-187-245-73-217.ngrok-free.app'],
      },
};

export default nextConfig;
